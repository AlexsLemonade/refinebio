class Processor:
    def __init__(self, **kwargs):
        self.id = (0,)
        self.name = ("string",)
        self.version = ("string",)
        self.docker_image = ("string",)
        self.environment = {}
