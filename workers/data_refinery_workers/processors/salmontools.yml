---  # "Salmontools" processor

# Operating system distribution (such as Ubuntu 16.04, CentOS 7.2, etc)
os_distribution:    # DO NOT EDIT this line

# OS-level packages (installed by OS package manager)
os_pkg:
  - python3
  - python3-pip
  - g++           # required to compile salmontools
  - cmake         # required to compile salmontools

# Command lines (assuming that "<cmd> --version" is supported)
cmd_line:
  - salmontools

# Python pip packages
python:
  - Django
  - data-refinery-common
