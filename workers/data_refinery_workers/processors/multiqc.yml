---  # "MultiQC" processor

# Operating system distribution (such as Ubuntu 16.04, CentOS 7.2, etc)
os_distribution:   # DO NOT forget the trailing ':'

# OS-level packages (installed by OS package manager)
os_pkg:
  - python3
  - python3-pip
  - default-jre   # required by fastqc


# Command lines (assuming "<cmd> --version" is supported)
cmd_line:
  - salmon
  - /home/user/FastQC/fastqc

# Python pip packages
python:
  - Django
  - data-refinery-common
  - multiqc
